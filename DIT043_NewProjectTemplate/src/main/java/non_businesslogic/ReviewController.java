package non_businesslogic;

// We implement item array and create review array,
// then create a new array that stores a review connected to a specific item


public class ReviewController {

//3.1 String itemID, String reviewComment, int reviewGrade

//3.7 Print items with most and least reviews

    /*
    To sort items with most reviews, I think what can be done is that first we need
    to compare the length of the arraylist Reviews

    initialize an empty arraylist of reviews

    ArrayList<Item> temp = new ArrayList<>();

    for (int i = 0; i < items.size; i++) {
        if (items.get(i).getReviews.size > items.get(i+1).getReviews.size) {
            temp.get(0) = items.get(i);
        }
        if (items.get(i).getReviews.size < items.get(i+1).getReviews.size) {
            temp.add(items.get(i+1));
        }
    }

    Item with the most reviews: return temp.get(i);
    Item with the least reviews: return temp.get(temp.size - 1)

     */
    
}
